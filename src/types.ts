export type OS_TYPE = { readonly link: string; readonly img: string };

export type ROUTES_TYPE = {
  readonly [key: string]: string;
};

export type profileType = {
  readonly name: string;
  readonly photo: string;
  readonly link: string;
};
